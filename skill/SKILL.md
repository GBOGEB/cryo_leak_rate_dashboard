---
name: cryo-leak-rate-level1
description: Use in cryo_leak_rate_dashboard to census, modernize, repair, innovate, or perpetuate leak-rate engineering capability; run Level-1 checks; coordinate independent physics/artifact receipts; and apply measured PCA or Bradley-Terry diagnostics without promoting derived calculations to bidder or QPS authority.
---

# Cryo Leak Rate Level 1

1. Read `LEVEL1.md` and `level1/ssot.json`.
2. Run `python level1/runtime.py census` and `python level1/runtime.py mip`.
3. Reuse `data/config.yaml`, build/validate scripts and `src/build_dashboard.py`; do not duplicate the native calculation/release path.
4. Run `python level1/runtime.py orchestrate` for blocks, agents, role and federation targets.
5. PCA accepts measured numeric rows only; BT accepts explicit winner/loser pairs only. Missing/insufficient input DEFERs.
6. Run `python level1/runtime.py self-test` before any Level-1.0 claim. Synthetic fixtures only prove the implementation.
7. Local leak-rate results remain independent evidence until a governed parent/child chain accepts their source class and use.
